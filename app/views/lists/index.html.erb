<!DOCTYPE html>
<html>

  <body>

    <p id="notice"><%= notice %></p>

    <div>
      <div>
        <h1>LISTS</h1>
      </div>

      <div>
        <% @lists.each do |list| %>
          <p><%= list.status %> List <span><%= link_to 'Show', list %></span></p>
        <% end %>
      </div>

      <!-- <#%= link_to 'New List', new_list_path %>  -->

      <div>
        <p>Website by Cindy Mendes</p>
      </div>

    </div>

     </body>
</html>








<!-- ------------------------------------------------------------------------------------------
<p id="notice"><#%= notice %></p>

<h1>Lists</h1>

<table>
  <thead>
    <tr>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <#% @lists.each do |list| %>
      <tr>
        <td><#%= list.status %></td>
        <td><#%= link_to 'Show', list %></td>
        <td><#%= link_to 'Edit', edit_list_path(list) %></td>
        <td><#%= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <#% end %>
  </tbody>
</table>

<br>

<#%= link_to 'New List', new_list_path %> -->
