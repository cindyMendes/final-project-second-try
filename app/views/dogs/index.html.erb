<!DOCTYPE html>
<html>

  <body>
    <p id="notice"><%= notice %></p>

    <div>
      <h1>List of All the Dogs Missing</h1>
    </div>

    <span><%= link_to 'Add New Dog', new_dog_path %></span>

    <div>
       <% @dogs.each do |dog| %>
         <% if dog.image? %>
           <p><%= image_tag dog.image.small.url%></p>
         <% end %>
         <p>Dog ID: <%= dog.id %></p>
         <p>Status: <%= dog.list.status %></p>
         <p> Breed: <%= dog.breed %></p>
         <p> Color: <%= dog.color %></p>
         <p> Size: <%= dog.size %></p>
         <p> Location: <%= dog.location %></p>
         <p> Date: <%= dog.date.strftime("%m/%d/%Y") %></p>
         <p> Description: <%= dog.description %></p>

         <span><%= link_to 'Show', dog %></span>
         <span><%= link_to 'Edit', edit_dog_path(dog) %></span>
         <span><%= link_to 'Delete', dog, method: :delete, data: { confirm: 'Are you sure?' } %></span>

       <% end %>
     </div>

     <span><%= link_to 'Add New Dog', new_dog_path %></span>

   </body>
</html>


<!-- ---------------------------------------------------------------------------------------------------------------------------- -->


<!-- <p id="notice"><#%= notice %></p> -->
<!--
<h1>Dogs</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Breed</th>
      <th>Color</th>
      <th>Size</th>
      <th>Location</th>
      <th>Date</th>
      <th>Description</th>
      <th>List</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <#% @dogs.each do |dog| %>
      <tr>
        <td><#%= dog.image %></td>
        <td><#%= dog.breed %></td>
        <td><#%= dog.color %></td>
        <td><#%= dog.size %></td>
        <td><#%= dog.location %></td>
        <td><#%= dog.date %></td>
        <td><#%= dog.description %></td>
        <td><#%= dog.list %></td>
        <td><#%= dog.user %></td>
        <td><#%= link_to 'Show', dog %></td>
        <td><#%= link_to 'Edit', edit_dog_path(dog) %></td>
        <td><#%= link_to 'Destroy', dog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <#% end %>
  </tbody>
</table>

<br>

<#%= link_to 'New Dog', new_dog_path %> -->
